# Crypto Trade Flow Pipeline
Containerized pipeline ingesting crypto currency market trades. Calculates aggregate data from a stream over a configurable window of `n` seconds.

## To run
```
docker compose build
docker compose up -d
```
